apiVersion: v1
kind: Service
metadata:
  name: example-app
spec:
  selector:
    app: example-app
  ports:
  - port: 10000        # External port for Envoy
    targetPort: 10000 # Envoy listens here
    protocol: TCP
    name: http
